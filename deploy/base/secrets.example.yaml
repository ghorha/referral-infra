# Create real secrets (do not commit values):
# kubectl -n referral create secret generic referral-db \
#   --from-literal=SPRING_DATASOURCE_URL='jdbc:postgresql://ep-...-pooler.../neondb?sslmode=require' \
#   --from-literal=SPRING_DATASOURCE_USERNAME='neondb_owner' \
#   --from-literal=SPRING_DATASOURCE_PASSWORD='...' \
#   --from-literal=DATABASE_URL='jdbc:postgresql://...' \
#   --from-literal=DATABASE_USER='neondb_owner' \
#   --from-literal=DATABASE_PASSWORD='...'
#
# kubectl -n referral create secret generic referral-redis \
#   --from-literal=SPRING_DATA_REDIS_HOST='...' \
#   --from-literal=SPRING_DATA_REDIS_PORT='6379' \
#   --from-literal=SPRING_DATA_REDIS_PASSWORD=''
#
# kubectl -n referral create secret generic referral-jwt \
#   --from-literal=JWT_SECRET='replace-with-32+-byte-secret'
