# Referral deployment (Phoenix + Neon + Vercel)

Modeled on piraho/fs-infra. Region locked to **us-phoenix-1**.

## Org secrets

`OCI_CLI_USER`, `OCI_CLI_TENANCY`, `OCI_CLI_FINGERPRINT`, `OCI_CLI_KEY_CONTENT`,
`OCI_CLI_REGION=us-phoenix-1`, `OKE_CLUSTER_OCID`, plus Vercel secrets on frontend repo.
